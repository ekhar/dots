# Linux-specific tmux configuration

# Status bar without meetings (no icalBuddy on Linux)
set -g @catppuccin_status_modules_right "directory date_time"

# Linux-specific sessionx paths
set -g @sessionx-custom-paths '$HOME/dots,$HOME/code'
